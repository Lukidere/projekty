
//
// use reqwest::blocking::Client;
// use tokio;
// use std::net::TcpStream;
// use std::error::Error;
// async fn main_connection<'a>(url:&'a str,target:&'a str) -> Result<(),Box<dyn Error>> {
//
//     let http_client = reqwest::get(&format!("https://crt.sh/?q=%25.{}&output=json",url)).await?;
//     let dane = http_client
//     Ok(())
// }
